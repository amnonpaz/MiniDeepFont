# sample = 16
# input_test(filename, sample, do_norm=False)
# input_test(filename, sample, do_norm=True)
# input_test(filename, sample, do_norm=True, affine_crop=True)
# x = np.append(bb[0, :], bb[0, 0])
# y = np.append(bb[1, :], bb[1, 0])
# points = np.array([x[:4], y[:4]]).T
# font = Preprocess.extract_font_crop_from_image_affine(img, points, (105, 105))
#    print('X: {X}'.format(X=x))
#    print('Y: {Y}'.format(Y=y))
##    plt.plot(x, y, bb_color(fonts[idx]))

#    plt.show()
